# Production dependencies only - optimized for Render deployment
#
# REMOVED from original requirements.txt to speed up deployment:
# - ipykernel, ipython, jupyter_* (Jupyter notebook - dev only) ~10MB
# - matplotlib, pillow, fonttools, kiwisolver, contourpy, cycler (plotting - not used in backend) ~20MB
# - debugpy, pexpect, ptyprocess (debugging/dev tools) ~5MB
# - prompt_toolkit, Pygments, jedi, parso, wcwidth (IPython UI) ~5MB
# - asttokens, executing, pure_eval, stack-data, decorator (IPython deps) ~2MB
# - appnope (macOS-only for IPython)
# - psutil, nest-asyncio, tornado, pyzmq, comm, traitlets, platformdirs (Jupyter deps)
# - packaging, pyparsing (unused utility libs)
#
# Total reduction: ~42MB of dependencies removed
# Expected deployment time: 1-2 minutes (down from 4+ minutes)

# Core FastAPI
fastapi==0.115.5
uvicorn==0.32.1
starlette==0.41.3
pydantic==2.10.2
pydantic_core==2.27.1

# Data processing (required)
pandas==2.2.3
numpy==2.1.3

# FastAPI dependencies
anyio==4.6.2.post1
sniffio==1.3.1
h11==0.14.0
click==8.1.7
idna==3.10
typing_extensions==4.12.2
annotated-types==0.7.0

# Pandas dependencies
python-dateutil==2.9.0.post0
pytz==2024.2
tzdata==2024.2
six==1.16.0
